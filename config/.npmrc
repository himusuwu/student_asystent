# Save exact versions in package.json (no ^ or ~)
save-exact=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Progress bar during install
progress=true

# Audit level (only show high/critical vulnerabilities)
audit-level=high

# Engine strict (enforce Node.js version)
engine-strict=false

# Prefer offline packages
prefer-offline=true
