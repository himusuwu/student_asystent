# Student Assistant - Example Environment Variables
# Copy this file to .env and fill in your actual values

# ========================================
# GitHub Integration (Optional)
# ========================================
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_yourPersonalAccessTokenHere
GITHUB_REPO=your-username/your-repo-name

# ========================================
# Backend Configuration
# ========================================
# Port for the transcription backend server
PORT=3001

# Node environment (development or production)
NODE_ENV=development

# ========================================
# AI Model Configuration (Optional)
# ========================================
# Whisper model to use (tiny, base, small)
WHISPER_MODEL=base

# Maximum number of threads for CPU inference
WHISPER_THREADS=8

# ========================================
# Frontend Configuration (Optional)
# ========================================
# Frontend server port (for http.server)
FRONTEND_PORT=8000

# ========================================
# Database Configuration (Optional)
# ========================================
# IndexedDB database name (usually doesn't need changing)
DB_NAME=studentAssistant

# Database version
DB_VERSION=2

# ========================================
# Security (Production Only)
# ========================================
# CORS allowed origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# API rate limit (requests per 15 minutes)
# RATE_LIMIT=100

# ========================================
# Monitoring (Optional)
# ========================================
# Sentry DSN for error tracking
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ========================================
# Notes:
# ========================================
# - Never commit .env file to git (already in .gitignore)
# - Most settings have sensible defaults and don't need to be set
# - Only GITHUB_TOKEN and GITHUB_REPO are needed for sync functionality
# - Backend works fine without any environment variables
